version: v2

# Vendored until the image catalog, hook removal and ImagePullSecret removal
# land in buf.build/agynio/api. Delete this directory and the directory input
# from buf.gen.yaml once they are published.
modules:
  - path: .

lint:
  use:
    - STANDARD
  except:
    - RPC_REQUEST_RESPONSE_UNIQUE
    - SERVICE_SUFFIX
